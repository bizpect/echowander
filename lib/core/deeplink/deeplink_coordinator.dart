class DeepLinkCoordinator {
  Future<void> enqueue(Uri uri) async {
    // TODO: store pending deep links until prerequisites are met.
  }

  Future<void> handlePending() async {
    // TODO: drain pending deep links when ready.
  }
}
